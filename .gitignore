.DS_Store
**/.DS_Store
design/.env
prime2026.sql

# Runtime uploads belong to persistent server storage, never to a deployment.
# Keep directory placeholders only, so the upload targets exist after checkout.
/uploads/**
!/uploads/**/
!/uploads/**/.gitkeep
